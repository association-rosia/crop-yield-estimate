name: crop-yield-estimate-env
dependencies:
  - python=3.10.*
  - pip
  - pip:
      - jupyter
      - scikit-learn
      - wandb
      - pandas
      - numpy
      - GPUtil
      - xgboost
      - plotly
      - matplotlib
      - pytorch-tabnet